export const metadata = {
  title: 'Supplier Login - Core Collective',
  description: 'Sign in to your Core Collective supplier dashboard to manage products, orders, and inquiries.',
};

export default function SupplierLoginLayout({ children }) {
  return children;
}
