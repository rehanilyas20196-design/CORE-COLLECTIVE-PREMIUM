export const metadata = {
  title: 'Notifications - Core Collective',
  description: 'View your notifications and updates from Core Collective marketplace.',
  robots: { index: false, follow: false },
};

export default function NotificationsLayout({ children }) {
  return children;
}
