export const metadata = {
  title: 'Admin Panel - Core Collective',
  description: 'Core Collective admin dashboard for managing orders, products, inquiries, notifications, and marketplace operations.',
  robots: { index: false, follow: false },
};

export default function AdminLayout({ children }) {
  return children;
}
